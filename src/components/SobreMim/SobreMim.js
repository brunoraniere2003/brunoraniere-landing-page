import React from 'react';
import { SobreContainer, SectionTitle, BioText } from './SobreMimStyle';

const SobreMim = () => (
  <SobreContainer>
    <SectionTitle>Sobre Mim</SectionTitle>
    <BioText>
      Trajetória marcada por paixão pela tecnologia e inovação constante.
      Busco transformar ideias em soluções digitais impactantes.
    </BioText>
  </SobreContainer>
);

export default SobreMim;
